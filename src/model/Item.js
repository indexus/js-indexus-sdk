import { Element } from "./Element.js";

class Item extends Element {
  /**
   * Returns the unique identifier of this item.
   * @returns {string}
   */
  id() {
    return "";
  }
}

export { Item };
