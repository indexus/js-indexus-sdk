class Mask {
  /**
   * Retrieves the mask value at the specified indices.
   * @param {number} i
   * @param {number} j
   * @returns {number}
   */
  at(i, j) {
    return 0;
  }

  /**
   * Returns a unique signature for the mask.
   * @returns {string}
   */
  signature() {
    return "";
  }
}

export { Mask };
