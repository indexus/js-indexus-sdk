class Point {
  /**
   * Returns the value(s) representing the point.
   * @returns {number[]}
   */
  value() {
    return [];
  }

  /**
   * Returns a string representation of the point.
   * @returns {string}
   */
  print() {
    return "";
  }
}

export { Point };
