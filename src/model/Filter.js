class Filter {
  // Base filter class; specific implementations may add properties
}

class Direction {
  /**
   * Returns the directional values.
   * @returns {number[]}
   */
  value() {
    return [];
  }
}

export { Filter, Direction };
